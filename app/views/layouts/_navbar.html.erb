    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'New Job', root_path, class: "navbar-brand" %>
          
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Home', root_path, class: "navbar=brand" %></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
          </ul>
          <ul class="navbar-right">
           <% if user_signed_in? %>
            <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-info navbar-btn" %>
            <%= link_to "Sign Out", destroy_user_session_path, class: "btn btn-warning navbar-btn", method: :delete %>
            <%= link_to "Post a Job", new_gig_path, class: "btn btn-success" %>
            <% else %>
             <%= link_to "Sign In", new_user_session_path, class: "btn btn-defalt navbar-btn" %>
             <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-defalt navbar-btn" %>
          <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>

